<!-- Navigation -->
<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header page-scroll">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand supercell-font" href="{{ path('coc', {'id_clan': clan.id }) }}">{{ clan.name }}</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                {% if active == "home" %} <li class="active"> {% else %} <li>{% endif %}<a href="{{ path('coc', {'id_clan': clan.id }) }}">Home</a></li>

                {% if active == "classement" %} <li class="active dropdown"> {% else %} <li class="dropdown">{% endif %}
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Classements <span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        <li><a href="{{ path('coc_players', {'id_clan': clan.id, 'type': '0' }) }}"> Classement joueurs   </a></li>
                        <li><a href="{{ path('coc_players', {'id_clan': clan.id, 'type': '1' }) }}"> Joueurs actifs   </a></li>

                        <li><a href="{{ path('coc_imagesBestAttack', {'id_clan': clan.id }) }}">Meilleures attaques  <span class="badge">{{ numberBestAttacks }}</span>  </a></li>

                    </ul>
                </li>

                {% if active == "base" %} <li class="active dropdown"> {% else %} <li class="dropdown">{% endif %}
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><span class="badge"> {{ numberBases }}</span> Plans de base <span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        <li><a href="{{ path('coc_images_base_add', {'id_clan': clan.id }) }}">Ajouter une base</a></li>
                        <li class="divider"></li>
                        <li><a href="{{ path('coc_images_base_category', {'id': 5, 'id_clan': clan.id} ) }}">HDV 5</a></li>
                        <li><a href="{{ path('coc_images_base_category', {'id': 6, 'id_clan': clan.id} ) }}">HDV 6</a></li>
                        <li><a href="{{ path('coc_images_base_category', {'id': 7, 'id_clan': clan.id} ) }}">HDV 7</a></li>
                        <li><a href="{{ path('coc_images_base_category', {'id': 8, 'id_clan': clan.id} ) }}">HDV 8</a></li>
                        <li><a href="{{ path('coc_images_base_category', {'id': 9, 'id_clan': clan.id} ) }}">HDV 9</a></li>
                    </ul>
                </li>

                {% if active == "media" %} <li class="active dropdown"> {% else %} <li class="dropdown">{% endif %}
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><span class="badge">{{ numberBestAttacks + numberVideos + numberImagesBonus }}</span> Média <span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        <li><a href="{{ path('coc_videos', {'id_clan': clan.id }) }}"><span class="badge">{{ numberVideos }}</span> Vidéos </a></li>
                        <li><a href="{{ path('coc_imagesBonus', {'id_clan': clan.id }) }}"><span class="badge">{{ numberImagesBonus }}</span> Bonus </a></li>
                    </ul>
                </li>

                {% if is_granted('ROLE_USER') %}
                    {%  if app.user.player.level is defined %}
                        <li><a href="{{ path('coc_player_edit', {'id': app.user.player.id ,'id_clan': clan.id }) }}"><b>Editer mon joueur</b></a></li>
                    {% endif %}
                {% endif %}

            </ul>

            <ul class="nav navbar-nav navbar-right">
                <li class="hidden">
                    <a href="#page-top"></a>
                </li>

                {% if is_granted('ROLE_USER') %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">{{ app.user.username }}
                            {% if is_granted('ROLE_ADMIN') %}
                                <span class="badge">{{  numberUsersNonAssigned }}</span>
                            {% endif %}
                            <span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">

                            {% if is_granted('ROLE_ADMIN') %}
                                <li><a target="_blank" href="{{ path('admin', {'id_clan': clan.id }) }}">Administration</a></li>
                            {% endif %}
                            {% if is_granted('ROLE_USER') %}
                                <li><a href="{{ path('fos_user_security_logout') }}">Déconnexion</a></li>
                            {% endif %}


                        </ul>
                    </li>

                {% else %}
                    <li class="page-scroll">
                        <a href="{{ path('fos_user_registration_register') }}">S'inscrire</a>
                    </li>
                    <li class="page-scroll">
                        <a href="{{ path('fos_user_security_login') }}">Login</a>
                    </li>
                {% endif %}
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
